<div>

<%= form_for @new_work, html: {multipart: true} , url: user_collection_works_path do |f| %>


<!-- 	<div ng-file-drop="onFileSelect($files)" ng-file-drag-over-class="optional-css-class-name-or-function">
		DRAG FILES HERE
	</div> -->

	<%= f.label :image %>
	<%= f.file_field :image, required: true, data: {:"ng-file-select" => "onFileSelect($files)"} %>

	 <i ng-hide="imageSrc">No image choosed</i>
   <img ng-src="{{imageSrc}}" /><br/>

	<img ng-src="">


	<a href="#" class="add-details" ng-init="details = false" ng-click="details = !details">Add Details</a>



	<div ng-show="details">


	<%= f.label :title %>
	<%= f.text_field :title, required: true %>

	<%= f.label :medium_names, 'Media' %>
	<%= f.text_field :medium_names, value: '' %>

	<%= f.label :genre_names, 'Genres' %>
	<%= f.text_field :genre_names, value: '' %>

	<%= f.label :caption %>
	<%= f.text_field :caption %>

	<%= f.submit 'Publish' %>


	</div>


<% end %>

</div>